[project]
dependencies = [
  "alembic>=1.14",
  "asyncpg>=0.30",
  "fastapi>=0.115",
  "httpx>=0.28",
  "numpy>=2.1",
  "opentelemetry-exporter-otlp-proto-grpc>=1.29",
  "opentelemetry-instrumentation-fastapi>=0.50b0",
  "opentelemetry-instrumentation-httpx>=0.50b0",
  "opentelemetry-instrumentation-sqlalchemy>=0.50b0",
  "opentelemetry-sdk>=1.29",
  "pydantic>=2.9",
  "pydantic-settings>=2.6",
  "redis>=5.2",
  "sqlalchemy[asyncio]>=2.0.36",
  "uvicorn[standard]>=0.34",
  "xgboost>=2.1",
]
description = "ML-based probability calibration and prediction service"
name = "prediction-engine"
requires-python = ">=3.12"
version = "0.1.0"

[project.optional-dependencies]
train = ["nba_api", "pandas", "pyarrow", "scikit-learn"]

[dependency-groups]
dev = [
  "ruff>=0.15",
  "mypy>=2.1",
  "pytest>=9.1",
  "pytest-asyncio>=1.4",
  "pytest-cov>=7.1",
  "pip-audit>=2.10",
  "pyyaml>=6.0",
  "respx>=0.23",
  "scikit-learn>=1.9",
  "testcontainers[postgres,redis]>=4.14",
  "types-pyyaml>=6.0.12",
]

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true

[[tool.mypy.overrides]]
ignore_missing_imports = true
module = ["opentelemetry.instrumentation.*", "sklearn.*", "pandas.*"]

[[tool.mypy.overrides]]
disallow_untyped_calls = false
module = ["prediction_engine.core.model.xgb", "prediction_engine.core.training.*"]

[tool.pytest.ini_options]
asyncio_default_fixture_loop_scope = "session"
asyncio_mode = "auto"
pythonpath = ["src", "."]
testpaths = ["tests"]
